# Ayo Les Portal Web (Teacher)

AyoLesPortal is web application portal for teacher for simple online course

* how to run via local fo golang dev

```

del main.exe && go build main.go && main.exe --config=.server.toml

```